# Feature → Code Generator (Next.js + Mustache)

## Run
```bash
rm -rf .next
yarn install
yarn dev
# open http://localhost:3000
```

Click **Generate ZIP** to download a React Native (Expo) starter based on your selections.
