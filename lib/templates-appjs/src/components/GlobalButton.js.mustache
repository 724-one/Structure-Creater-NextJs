import React from 'react';
import { Pressable, Text } from 'react-native';

export default function GlobalButton({ title = 'Button', onPress, style, textStyle }) {
  return (
    <Pressable onPress={onPress} style={[{ backgroundColor: '#111', paddingVertical: 12, paddingHorizontal: 16, borderRadius: 12 }, style]}>
      <Text style={[{ color: 'white', fontWeight: '600', textAlign: 'center' }, textStyle]}>{title}</Text>
    </Pressable>
  );
}
